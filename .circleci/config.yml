version: 2.1

orbs:
  aws-ecr: circleci/aws-ecr@9.3.7

workflows:
  build_and_push_image:
    jobs:
      - aws-ecr/build-and-push-image:
          context: aws-dev
          create-repo: true
          dockerfile: Dockerfile
          path: .
          repo: dev/circlecir-repo
          tag: "$CIRCLE_SHA1"
